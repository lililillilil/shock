/**
 * Show
 */
class Show {
    /**
     * @param {Array} devices
     * @param {Array} commands
     */
    constructor(devices, commands) {
        this.devices = devices;
        this.commands = commands;
    }
}

module.exports = Show;
